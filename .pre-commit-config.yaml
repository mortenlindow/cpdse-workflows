repos:
  - repo: local
    hooks:
      - id: validate-vocab
        name: Validate vocab + crosswalk + model doc consistency
        entry: python3 scripts/validate-vocab.py
        language: system
        pass_filenames: false
        files: '^(reference/(vocab|pharma-value-chain-model)/|scripts/validate-vocab\.py$)'
